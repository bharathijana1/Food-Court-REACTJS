import React from 'react'

const Headings = ( { heading } ) => {
  return (
    <div>
        <h1 className='text-center font-semibold p-3 text-2xl uppercase drop-shadow-lg '>{heading}</h1>
    </div>
  )
}

export default Headings
